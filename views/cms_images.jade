
div.images
  table
    each image in images
      tr
        td.image
          img(src="#{image.path}") 
        td.path
          #{image.path} 
        td.delete
          a(href="javascript:void(0);") Delete
